{
  "name": "@omts/quick-sort",
  "version": "0.0.1",
  "main": "dist/index.cjs.js",
  "module": "dist/es/index.js",
  "browser": "dist/index.umd.js",
  "unpkg": "dist/index.umd.js",
  "types": "dist/es/index.d.ts",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "scripts": {
    "clean": "rimraf dist",
    "dev": "bun run clean && bun build src/index.ts --outdir dist --watch",
    "build": "bun run clean && bun run build:cjs && bun run build:esm && bun run build:umd && bun run build:types",
    "build:esm": "bun build src/index.ts --outdir dist/es ",
    "build:cjs": "esbuild src/index.ts --bundle --outfile=dist/index.cjs.js --format=cjs",
    "build:umd": "esbuild src/index.ts --bundle --outfile=dist/index.umd.js --format=iife --global-name=OmtsQuickSort",
    "build:types": "tsc --emitDeclarationOnly --outDir dist/es",
    "test": "bun test"
  },
  "devDependencies": {
    "bun-types": "^1.1.27",
    "esbuild": "^0.23.1",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.2"
  }
}