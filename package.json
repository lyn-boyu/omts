{
  "name": "omts",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "publish": "pnpm -r test && pnpm -r publish --access public",
    "test": "lerna run test --stream"
  },
  "devDependencies": {
    "@types/node": "^22.5.5",
    "bun-types": "^1.1.27",
    "lerna": "^8.1.8",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.2"
  }
}
